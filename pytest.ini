[pytest]
# Make `import app` work by adding backend to sys.path
pythonpath = backend

# Limit test discovery to backend tests
testpaths = backend/tests

# Nice defaults (opt-in verbosity can be added on CLI)
addopts = -ra
