# Core FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.5.2
python-multipart==0.0.6
aiofiles>=0.24.0
python-dotenv>=1.0.0
email-validator>=2.1.0  # For Pydantic EmailStr validation

# HTTP client
httpx>=0.27.0
urllib3>=1.26.0
requests>=2.31.0  # TEMPORARY: Used by SAM.gov scripts, remove when native SAM integration is built

# OpenAI / LLM (unchanged)
openai>=1.35

# ZIP utilities
zipfile38>=0.0.3

# Optional queueing (if you enabled Celery/Redis in v2)
celery>=5.3
redis>=5.0
watchdog>=3.0

# Database (SQLAlchemy with async support)
sqlalchemy[asyncio]==2.0.25
alembic==1.13.1
asyncpg==0.29.0  # PostgreSQL async driver
aiosqlite==0.19.0  # SQLite async driver
psycopg2-binary==2.9.9  # PostgreSQL sync fallback

# Authentication & Security
pyjwt==2.8.0
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Email
aiosmtplib>=3.0.0  # Async SMTP client
jinja2>=3.1.3  # Email template rendering
sendgrid>=6.11.0  # Optional: SendGrid backend
boto3>=1.34.0  # Optional: AWS SES backend

# Object Storage (MinIO / S3)
minio>=7.2.0  # MinIO Python SDK for S3-compatible storage

# PDF Generation (for SAM.gov summaries)
markdown>=3.5.0  # Markdown to HTML conversion
weasyprint>=60.0  # HTML to PDF conversion
